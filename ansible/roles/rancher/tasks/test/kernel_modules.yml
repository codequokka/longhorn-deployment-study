---
- name: Check iptalbe modules are loaded
  ansible.builtin.shell:
    cmd: |
      set -o pipefail
      lsmod | grep '^{{ module }}\>'
  loop_control:
    loop_var: module
  loop: "{{ rancher_iptables_modules }}"
  changed_when: false
